This directory allows Pagination to work on the page {url}/galleries

Pagination does not work on markdown pages, instead it requires that it be seeded
on an index.html page. In order to work on a different named page, the index.html
page must be situated inside a directory of the desired name.
